# Sentiment-Aware IT Support Assistant - MSAI-631 group project
gradio>=4.44.0
scikit-learn>=1.3.0
transformers>=4.40.0
torch>=2.0.0

# Notes on graceful degradation:
#   scikit-learn is REQUIRED (TF-IDF retrieval).
#   transformers and torch are OPTIONAL. Without them the application still runs:
#   the emotion classifier falls back to a documented lexicon and the response
#   generator falls back to a deterministic template composer. The interface
#   reports which backend is active on startup.
